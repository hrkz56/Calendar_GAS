# ちゃっぴーカレンダー 正本差分（要反映）

現在の正本Ver1.0に対し、今回のチャットで確定・実装した内容として次を反映する。

## 第2章 予定分類・表示への追加・変更

- 休みはピンク色で管理する。
- 有給は `有給休暇` / `午後休` / `2時間休` の3種類とし、ピンク色で管理する。
- 健康カテゴリを設け、緑色で管理する。
  - 例: `【健康】ジム（セミパ）`
  - 例: `【健康】ジム（エニタイム）`
- 終日カテゴリを設け、黄色で管理する。
  - 例: `資産負債入力`

## GAS運用ルールへの追加

- Calendar_GASの実装ファイルは `.gs` を正とし、`.js` と混在させない。
- UIからGAS関数へ渡す引数数・順序は変更時に必ず整合確認する。
- `clasp push` 成功後、Webアプリは新バージョンで再デプロイしてから実機確認する。
